# The Red Ledger — 채무 트랙 설계 분석

> 문제: 갚는 쪽에는 **신용도 사다리**(누적 상환액 300/600/900/1200 + 이후 무한 보너스)가 있는데,
> 빚을 굴리는 쪽에는 런 단위 진행 트랙이 없다. v0.17 이 카드 3장(담보·부도 위기·레버리지)을 넣어
> **순간 화력**은 열었지만 **진행 보상**은 여전히 한쪽에만 있다.
> 목표: 소개글의 "언제 털 것인가"를 **양쪽 다 진행 보상이 있는 결정**으로 만든다.
> 작성 2026-07-29. 코드 기준 v0.18.3.

---

## 0. 결론 먼저

**최초 제안이었던 "최고 원금(peak principal) 도달치"는 지표로 틀렸다.** 코드를 읽고 뒤집었다.
올바른 지표는 **채무 적분** — `Σ(방마다 지고 있던 빚)`. 이유는 §1.

| | 최고 원금 | **채무 적분** |
|---|---|---|
| 무엇을 재나 | 한 번에 얼마나 크게 빌렸나 | 얼마나 크게 **×** 얼마나 오래 졌나 |
| 빠른 순환형도 얻나 | **얻는다** (실측 825 한 방) | 거의 못 얻는다 (갚으면 시계가 멈춤) |
| 신용도 사다리와 배타적인가 | ❌ 아니다 | ✅ 그렇다 |
| 스톨링 파밍 | 어음+채무 조정으로 성립 | 성립 안 함 (방 단위라 전투 스톨링 무효) |

---

## 1. 왜 최고 원금은 틀렸는가

**신용도 사다리를 최적으로 타는 플레이어가 최고 원금도 동시에 최대화한다.**

근거는 코드 주석에 이미 실측으로 적혀 있다 — `LoanRecord.CreditPaid`:

> 인출 3회 + 금액 상한 없음이라 유물 3개를 한 번에 지르면 갚을 돈이 900을 넘고,
> 그 **한 번의 청산으로 고정 사다리를 통째로 건너뛴다**(실측: 825 빌리면 이자 포함 925 → 신용도 9).

즉 "크게 빌린다"는 **이미 신용도 루트의 최적 행동**이다. 여기에 최고 원금 트랙을 얹으면
두 사다리를 **같은 한 번의 행동으로 동시에** 타게 된다 → 선택이 아니라 공짜 보너스가 된다.
`LumpSumCreditCap`(600)이 신용도 쪽에서 바로 이 문제를 막으려고 도입된 가드인데,
최고 원금 트랙은 그 가드가 막은 행동을 **다시 보상**하는 꼴이다.

**추가 결함**: 최고 원금은 고수위선(high-water mark)이라 **되돌릴 수 있다**.
`채무 조정`(원금 250 탕감, 대출당 1회)이 골드를 쓰지 않으므로
`어음`(0코 소멸, 원금 +100) ×3 = +300 → 채무 조정으로 250 탕감 → **순 비용 50골드로 peak +300**.
청산하면 `RestructuringUsed` 가 리셋되므로(레코드와 함께 소멸) 사이클마다 반복 가능하다.

---

## 2. 올바른 지표 — 채무 적분

```
DebtLoad += Principal / 100     (방을 하나 지날 때마다, 빚이 있는 동안만)
```
단위 = **"100골드-방"**. 빚 400을 10방 지고 다니면 40.

- **갚으면 시계가 멈춘다** → 빠른 순환형은 거의 못 쌓는다. 신용도 사다리와 진짜로 배타적.
- **전투 스톨링이 무효** — 방 단위라 턴을 끄는 것으로는 1도 안 오른다.
  `DOPAMINE_BACKLOG` §4가 금지한 "턴을 끌수록 이득인 엔진"(취업알선 파워형 실패 전례)을 구조적으로 회피한다.
- **이미 있는 값으로 잰다**: `OnRoomEntered` → `AccrueNodeInterest` 가 매 방 전 플레이어를 돌면서
  `rec.Principal` 과 `roomsCarried` 를 이미 계산한다. 새 인프라가 사실상 없다.

### ⚠️이자를 지표로 재활용하면 안 된다
`NodeInterestGold` 는 그럴싸해 보이지만 **`InterestGoldCap`=100 에 하드 캡**이 걸린다.
게다가 origination 은 캡을 안 타므로(§3) **크게 빌릴수록 노드 이자가 0이 되는** 역전이 있다.
캡 없는 별도 카운터가 필요하다.

---

## 3. 수치 — 빚은 실제로 얼마나 커지나

### 원금 상한 (SP, 카드빚 제외)
`origination = amount × 20%` 는 **즉시·전액** 부과되고 **`InterestGoldCap` 을 타지 않는다**.
캡은 노드 이자에만 걸린다: `maxNodeGold = max(0, 100 − origination)`.

| 빌린 금액 | origination | 노드 이자 상한 | **최대 빚** |
|---|---|---|---|
| 100 | 20 | min(40%·100, 80) = 40 | **160** |
| 200 | 40 | min(80, 60) = 60 | **300** |
| 300 | 60 | min(120, 40) = 40 | **400** |
| 500 | 100 | min(200, **0**) = 0 | **600** |
| 825 (인출 3회 실측) | 165 | **0** | **990** |

> ★**크게 빌리면 노드 이자가 0이 된다.** 500 이상은 origination 만으로 캡을 넘겨
> 방을 아무리 지나도 이자가 안 붙는다 — 즉 **큰 빚을 오래 끄는 것이 방당 비용이 가장 싸다**.
> 채무 트랙은 정확히 그 행동을 보상하므로 이 기존 특성과 **같은 방향으로 증폭**된다. 캘리브레이션에서 감안할 것.

여기에 더해지는 것: 카드빚(장당 45~95, 방문당 `ShopCreditLimit` 120) · 전투 중 빚
(`어음` +100 · `저당`/`대출 강타` +30 · `취업알선` +20 — **상한 없음**).

### 채무 적분 구간
방 수 기준선은 코드에 있는 티어 일정 하나뿐이다 — `Schedule` = 13방 연체 / 17방 차압 / 22방 신용 불량
(주석: "대출 후 ~1막이면 전 저주 개방").

| 플레이 | 빚 × 방 | **적분** |
|---|---|---|
| 빠른 순환 (300 빌려 다음 상점서 청산) | 400 × 4 | **16** |
| 표준 (300, 12방) | 400 × 12 | **48** |
| 깊은 빚 (300, 22방 = 전 저주 개방) | 400 × 22 | **88** |
| 레버리지 커밋 (825, 25방) | 990 × 25 | **248** |
| 극단 (카드빚·어음 얹어 1400, 40방) | 1400 × 40 | **560** |

**제안 문턱: 50 / 120 / 250**
→ 각각 "표준인데 좀 끌었다" / "전 저주를 다 열고도 계속 간다" / "레버리지 빌드에 런을 걸었다".

---

## 4. 자기검증 — 파밍 경로 전수

| 경로 | 적분을 부풀릴 수 있나 | 판정 |
|---|---|---|
| 전투 스톨링(턴 끌기) | 방 단위라 **0** | ✅ 안전 |
| 어음/저당/대출 강타/취업알선으로 빚 키우기 | 오른다 — 단 **실제로 이고 다니는 동안만**. 이자·티어 저주·상환 부담을 전부 문다 | ✅ 대가 있음 |
| **채무 조정**(250 탕감) | 탕감은 이후 적분을 **줄인다**. 부풀릴 수 없다 | ✅ 안전 (최고 원금 안에서는 익스플로잇이었다) |
| 파산 선언 | 네이티브 Debt 만 소멸, `Principal` 불변 | ✅ 무관 |
| 맵에서 방 더 돌기 | 런의 방 수가 유한 + 방마다 티어 저주가 조여옴 | ✅ 자연 상한 |
| 청산 후 재대출 반복 | 적분은 **빚이 있는 동안만** 오르므로 사이클링이 오히려 손해 | ✅ 의도대로 |

**결론: 알려진 파밍 경로 없음.** 최고 원금 안에서 성립하던 어음+채무 조정 루프가 적분에서는 원천 봉쇄된다.

---

## 5. 구현 지점

| 항목 | 위치 | 비고 |
|---|---|---|
| 누적 | `LoanService.OnRoomEntered` (이미 매 방 전 플레이어 순회) | `AccrueNodeInterest` 옆. 가드(`Active && Principal > 0`)가 이미 그 안에 있다 |
| **멱등성** | `LastLoadFloor` 를 두고 `Principal × (TotalFloor − LastLoadFloor)` | ★`OnRoomEntered` 단순 `+=` 는 재발화/리로드에 취약하다 — `InterestPctApplied`/`NodeInterestGold` 가 목표값 방식으로 짜인 것과 **같은 이유**. 그 패턴을 따를 것 |
| 영속화 | `DebtLoanRelic` `[SavedProperty]` 2개 (`DebtLoad`, `LastLoadFloor`) | `CreditPaid` 와 동일 경로 (`SyncToRelic`/`RestoreFrom`) |
| 리셋 | **하지 않는다** — 청산해도 유지 (신용도와 대칭인 런 단위 트랙) | `ApplyRepay` 의 사이클 리셋 블록에 **넣지 말 것** |
| 수령 상태 | `DebtRewardsTaken`(개수) | 신용도의 `CreditRewardsTaken` 과 동형 — 순차 수령이라 개수 하나로 충분 |
| UI | 빚 상점 칩 (`NDebtCardShopPanel.cs:745` 의 신용 칩 패턴 복제) | 빚 상점은 **빚이 있어야 열리는데**, 채무 트랙이 진행 중이라는 건 곧 빚이 있다는 뜻 → 자리 궁합이 맞다 |
| co-op | `dl_sync claim` 과 동형 와이어 1개 | 카드 선택은 엔진이 동기화(`CardSelectCmd.ReserveChoiceId`). **재생이 detached 면 안 된다** |

---

## 6. 보상 설계

원칙 두 개.

**(a) 일반 파워를 주지 않는다.** 카드 강화/제거는 신용도 사다리의 몫이다. 채무 트랙은
**빚을 자산으로 바꾸는 것**만 준다 — 안 그러면 두 사다리가 같은 보상을 두고 경쟁한다.

**(b) ★유한하게 3칸.** 신용도 사다리는 무한(보너스 200골드마다)이지만 채무 트랙은 **유한이어야 한다.**
갚는 것은 **벌어야 하는 골드**를 쓰지만, 빚을 지고 있는 것은 **시간이 알아서 흐르는** 수동 행동이다.
수동 트랙을 무한으로 두면 장기 런에서 능동 트랙을 앞지른다.

| 칸 | 문턱 | 보상(안) | 근거 |
|---|---|---|---|
| 1 | 50 | 담보 / 레버리지 / 부도 위기 중 **1장 선택 지급** | 빚 빌드의 재료를 준다. 기존 카드 재사용이라 구현비 0 |
| 2 | 120 | **티어 저주 1종을 골라 면역** | `CHARACTER_SYNERGY` §3 — 차압은 디펙트·사일런트에게만 치명적이라 캐릭터 편차가 크다. 이 편차를 **플레이어 선택**으로 해소하므로 보상이자 밸런스 수정 |
| 3 | 250 | **대마불사(Too Big To Fail)** 유물 — 빚 100당 최대 HP +3 (상한 +30), 청산 시 되돌아감 | "덩치가 크면 안 망한다"는 테마. 상한이 없으면 극단 빌드에서 폭주 |

### 열린 질문
- **칸 2 면역이 티어 일정 자체를 무르게 하나** — 저주가 3종인데 하나를 빼면 22방 이후 압박이 2/3로 준다.
  적분이 계속 오르려면 빚을 이고 있어야 하는데 압박이 줄면 그게 쉬워진다 → 자기강화 고리 점검 필요.
- **칸 3 유물의 청산 시 되돌림** — 최대 HP가 줄 때 현재 HP 처리(초과분 잘림)를 어떻게 할지.
  잘못하면 청산이 즉사가 된다.
- **co-op 적분** — 인원별 개별 트랙인가 공유인가. 이자는 이미 인원수로 스케일하므로(5%×N) 개별이 자연스럽다.

---

## 7. 곁다리로 발견한 낡은 숫자 2건 — **해결됨(2026-07-29)**

> 두 건 다 정리했다. 2번은 단순 오기가 아니라 **유저에게 글로 한 약속이 깨져 있던 것**이었다.
> · 1번 → 로그가 `BorrowOriginationPct` 를 보간하도록 수정(다시 낡을 수 없다).
> · 2번 → **`interestGoldCap` 슬라이더 제거 + 캡 의미를 '노드 이자 상한'으로 확정**. 코드는 그대로 두고
>   서술을 코드에 맞췄다(§B). `925` 는 origination 이 캡을 탄다는 전제의 값이었으므로 **990** 으로 정정.
>   §3 표의 990 이 맞는 값이다.
> ⚠️**남은 판단 하나**: 실효 노드 상한은 `max(0, cap − origination)` 이라 순수한 '노드 상한'이 아니다.
> `− origination` 을 빼면 중형 대출의 이자가 오른다(300 빌리면 총 이자 100 → 160) — **밸런스 변경**이라
> 손대지 않았다. 순수 노드 캡으로 갈지는 별도 결정.

### 원문 기록

둘 다 v0.18.2 의 "50% surcharge"·"PrincipalRepayShare 0.2" 와 **같은 종류**다 — 코드가 바뀌었는데
숫자가 안 따라온 자리.

1. **`GrantLoan` 로그 문자열** (`LoanService.cs:511`): `owed {principal}=+30%` 인데 실제 origination 은
   **20%**(`BorrowOriginationPct`). 로그뿐이라 유저 영향은 없다.

2. **`CreditPaid` 주석의 실측치가 현 config 와 안 맞는다**: "실측: 825 빌리면 **이자 포함 925**" 라고
   적혀 있는데, 지금 계산은 `825 + round(825×20%) = 990` 이다(노드 이자는 origination 165 가 이미
   `InterestGoldCap` 100 을 넘겨 0). 925 가 나오려면 origination 이 ~12% 여야 한다.
   → **주석이 옛 요율 시절의 실측이거나, 측정 당시 인출액이 825 가 아니었다.**
   `LumpSumCreditCap`=600 이라는 결론 자체는 두 값(925/990) 어느 쪽이든 신용도 9로 같아 바뀌지 않지만,
   **이 숫자를 캘리브레이션 근거로 재인용하기 전에 재측정할 것.** §3 표의 990 은 계산값이지 실측이 아니다.
